{# Sidebar layout (.cui-sidebar). Use vars for --cui-sidebar-* overrides. Common layout props. #}
{% include '@CombatUICore/layouts/_layout.html.twig' with {
    base_class: 'cui-sidebar',
    style_vars: {},
} %}
