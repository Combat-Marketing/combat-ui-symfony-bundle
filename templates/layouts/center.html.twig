{# Viewport-centered block (.cui-center). Props: size (min-block-size via --cui-center-size) + common layout props. #}
{% include '@CombatUICore/layouts/_layout.html.twig' with {
    base_class: 'cui-center',
    style_vars: props.size is defined ? {'--cui-center-size': props.size} : {},
} %}
