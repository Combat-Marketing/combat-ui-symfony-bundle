{# Wrapping flex row (.cui-cluster). Props: gap + common layout props. #}
{% include '@CombatUICore/layouts/_layout.html.twig' with {
    base_class: 'cui-cluster',
    style_vars: props.gap is defined ? {'--cui-cluster-gap': props.gap} : {},
} %}
