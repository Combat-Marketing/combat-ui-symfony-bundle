{# Aspect-ratio media frame (.cui-frame). Use vars for --cui-frame-* overrides. Common layout props. #}
{% include '@CombatUICore/layouts/_layout.html.twig' with {
    base_class: 'cui-frame',
    style_vars: {},
} %}
