{#
Inline-axis container (.cui-container).
Props: width (narrow|wide — omit for the default width) + common layout props.
#}
{% set width = props.width|default(null) %}
{% include '@CombatUICore/layouts/_layout.html.twig' with {
    base_class: width ? 'cui-container-' ~ width : 'cui-container',
    style_vars: {},
} %}
