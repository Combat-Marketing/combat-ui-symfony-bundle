# Disable the global min-release-age cooldown for this repo.
# @combat-ui/core is a first-party package we publish and consume ourselves,
# so the supply-chain cooldown (which blocks freshly-published versions) only
# gets in the way of iterating on our own alpha releases.
min-release-age=0
